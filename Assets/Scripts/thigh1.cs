using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class thigh1 : MonoBehaviour
{
    public HingeJoint thigh1Joint;
    // Start is called before the first frame update
    void Start()
    {
        thigh1Joint = GetComponent<HingeJoint>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
