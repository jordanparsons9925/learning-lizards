using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class thigh2 : MonoBehaviour
{
    public HingeJoint thigh2Joint;
    // Start is called before the first frame update
    void Start()
    {
        thigh2Joint = GetComponent<HingeJoint>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
